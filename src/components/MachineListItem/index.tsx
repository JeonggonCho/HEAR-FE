import {FC} from "react";

import {Container} from "./style.ts";

const MachineListItem:FC = () => {
    return (
        <Container>
            item
        </Container>
    );
};

export default MachineListItem;