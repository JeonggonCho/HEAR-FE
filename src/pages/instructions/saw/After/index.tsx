import {FC} from "react";
import {Container} from "../../instruction.style.ts";

const After:FC = () => {
    return (
        <Container>
            톱 사용후
        </Container>
    );
};

export default After;