import {FC} from "react";
import {Container} from "../../instruction.style.ts";

const Warning:FC = () => {
    return (
        <Container>
            톱 주의사항
        </Container>
    );
};

export default Warning;